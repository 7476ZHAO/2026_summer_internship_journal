### Development Log – Avatar Repository Testing & Debugging

* Reviewed and tested multiple PRs in the Avatar repository.
* Created separate local Git branches for isolated PR testing workflows.
* Set up and used Python virtual environments for dependency management and runtime testing.
* Tested the manual drone controller module:

  * Verified drone connection and movement commands (takeoff, landing, directional controls).
  * Investigated camera streaming functionality.
  * Confirmed that `streamon` returned `ok`, but the camera view still displayed “No video”.
  * Capture functionality also appeared non-functional during testing.
* Reviewed frontend PR for BCI device switching:

  * Verified OpenBCI / Neurosity switch button integration.
  * Confirmed UI updates and log message behavior.
  * Checked for regressions and runtime stability.
* Investigated Avatar BCI backend architecture:

  * Traced QML frontend interactions with Python backend controllers.
  * Explored BrainFlow and OpenBCI integration flow.
  * Identified hardcoded macOS serial port configuration in OpenBCI setup logic.
  * Investigated backend connection flow and BrainFlow session initialization structure.
* Performed runtime log analysis and debugging of QML/Python integration issues.
